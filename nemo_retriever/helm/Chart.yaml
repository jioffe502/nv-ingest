apiVersion: v2
name: nemo-retriever
description: |
  Helm chart for the nemo-retriever ingest service (FastAPI) and its
  optional set of supporting NIM microservices.

  This chart deploys the "service" run mode of nemo-retriever as a single
  scalable web endpoint that orchestrates document ingestion against
  remote NIM endpoints (object detection, OCR, VLM embed).

  In-cluster NIMs are provisioned via NVIDIA NIM Operator custom
  resources (NIMCache + NIMService, apps.nvidia.com/v1alpha1). The
  templates short-circuit when the operator CRDs are absent, so the
  chart degrades gracefully into "external NIM URLs only" mode.

  Gateway scheduler state is process-local and ephemeral. Split mode uses one
  gateway replica with a Recreate strategy; gateway replacement loses accepted
  work and status history.

type: application
version: "26.5.0"
appVersion: "26.5.0"
kubeVersion: ">=1.25.0-0"
home: https://github.com/NVIDIA/NeMo-Retriever
sources:
  - https://github.com/NVIDIA/NeMo-Retriever
maintainers:
  - name: NeMo-Retriever maintainers
keywords:
  - NeMo-Retriever
  - nvidia
  - nim
  - document-ingestion
  - rag
  - retrieval
icon: https://www.nvidia.com/content/dam/en-zz/Solutions/about-nvidia/logo-and-brand/01-nvidia-logo-vert-500x200-2c50-d.png
annotations:
  category: AI
  licenses: Apache-2.0
